const mongoose = require("mongoose");

const projectUserSchema = new mongoose.Schema({
  email: {
    type: String,
    required: true,
  },
  password: {
    type: String,
    required: true,
  },
});

const User = mongoose.model("User", projectUserSchema);

module.exports = User;